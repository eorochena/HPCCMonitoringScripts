#!/bin/bash

status=$(service hpcc-init -c eclscheduler status|tail -1|grep eclscheduler|grep -v grep|awk '{print $NF}')

for state in $status
do
	if [ $status == 'stopped...' ]
	then
		echo "is stopped"
	elif [ $status == 'running...' ]
	then
		echo 'is running'
	fi
done
